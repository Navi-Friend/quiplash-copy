import { BasePlayer } from './player.abstract.entity';

export class VIPPlayer extends BasePlayer {
	// constructor(name: string) {
	// 	super(name, uuid4(), 0);
	// }

	// static createNew(name: string): VIPPlayer {
	// 	return new Player(name, uuid4());
	// }

	// static restore(data: PlayerModel): VIPPlayer {
	// 	return new Player(data.name, data.playerId, data.score);
	// }
}
